imports:
    - { resource: config.yml }
    - { resource: services_prod.yml }

#framework:
#    validation:
#        cache: apc

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        slacker:
            type: slackwebhook
            webhook-url: 'https://hooks.slack.com/services/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'
            channel: '#systemtest'
            level: error
            channels: ['!assetic'] # Ignore assetic errors, as these will cause a burst of error messagess on every cache clear.
        console:
            type:  console

assetic:
    use_controller: "%use_assetic_controller%"