{% extends "UKMDeltaBundle::layout.html.twig" %}

{% trans_default_domain translationDomain %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h2 class="mt-0">{{ 'whoAction.sporsmal'|trans }}</h2>

                <p class="my-4">
                    <a href="{{ path('ukm_delta_ukmid_pamelding_v2_opprett', { k_id: kommune.getId(), pl_id: arrangement.getId(), type: type.getKey(), hvem: 'alene' }) }}" class="btn btn-outline-primary btn-block">
                        {{ 'whoAction.alene'|trans }}
                    </a>
                </p>

                <p class="my-4">
                    <a href="{{ path('ukm_delta_ukmid_pamelding_v2_opprett', { k_id: kommune.getId(), pl_id: arrangement.getId(), type: type.getKey(), hvem: 'sammen' }) }}" class="btn btn-outline-primary btn-block">
                        {{ 'whoAction.sammen'|trans }}
                    </a>
                </p>

                <p class="my-4">
                    <a href="{{ path('ukm_delta_ukmid_pamelding_v2_opprett', { k_id: kommune.getId(), pl_id: arrangement.getId(), type: type.getKey(), hvem: 'kontakt' }) }}" class="btn btn-outline-primary btn-block">
                        {{ 'whoAction.kontakt'|trans({}, 'base') }}
                    </a>
                </p>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <p class="delta-subtext center">
                    <small>
                        {{ 'whoAction.forklaring'|trans({}, 'base') }}
                    </small>
                </p>
            </div>
        </div>
    </div>
{% endblock %}
